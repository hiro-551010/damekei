# Lissandra（リサンドラ）champion-passives.json

## champion-passives.json エントリ

なし（Meraki データをそのまま使用）。

## 各スキルの扱い

| スキル | 扱い |
|---|---|
| Q（Ice Shard） | magic。Meraki 値をそのまま使用 |
| W（Ring of Frost） | magic。Meraki 値をそのまま使用 |
| E（Glacial Path） | magic。Meraki 値をそのまま使用 |
| R（Frozen Tomb） | magic。Meraki 値をそのまま使用 |

## 対象外

| 項目 | 理由 |
|---|---|
| Passive（Iceborn Subjugation） | 敵キルで凍結ゾンビを生成。自身のダメージ出力に影響なし |
