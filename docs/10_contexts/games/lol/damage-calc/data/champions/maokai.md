# Maokai（マオカイ）champion-passives.json

## champion-passives.json エントリ

なし（Meraki データをそのまま使用）。

## 各スキルの扱い

| スキル | 扱い |
|---|---|
| Q（Bramble Smash） | magic。Meraki 値をそのまま使用 |
| W（Twisted Advance） | magic。Meraki 値をそのまま使用 |
| E（Sapling Toss） | magic。Meraki 値をそのまま使用 |
| R（Nature's Grasp） | magic。Meraki 値をそのまま使用 |

## 対象外

| 項目 | 理由 |
|---|---|
| Passive（Sap Magic） | 周囲でスペルが発動するたびに HP 回復。ダメージ出力に影響なし |
